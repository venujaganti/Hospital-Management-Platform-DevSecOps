apiVersion: v1
kind: Service

metadata:
  name: hospital-frontend
  namespace: hospital

spec:

  type: NodePort

  selector:

    app: hospital-frontend

  ports:

    - name: http

      protocol: TCP

      port: 80

      targetPort: 80

      nodePort: 30080